import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://rudra-g-23.github.io',
  base: '/dsa-for-data-science',
  trailingSlash: 'always',
  output: 'static',
});
